# ``conda env create --file environment.yml``
# or
# ``conda env create --name <myEnvName> --file environment.yml``

name: tposae
dependencies:
  - docopt
  - ipython
  - jupyter
  - matplotlib
  - numpy
  - pip
  - pyqt
  - pyqtgraph
  - python=3.7.6
  - conda-forge::qdarkstyle
  - conda-forge::hcipy>=0.5.1
  - pip:
    - qtvscodestyle
